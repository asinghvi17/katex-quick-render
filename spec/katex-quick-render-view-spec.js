'use babel';

import KatexQuickRenderView from '../lib/katex-quick-render-view';

describe('KatexQuickRenderView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
